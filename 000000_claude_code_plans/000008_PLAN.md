# Plan 008: Change Open WebUI image tag from "0.8" to "latest"

## Context
Use the `latest` tag for the Open WebUI Docker image instead of the pinned `0.8` tag.

## Change
- [x] In `oow_ctl.sh` line 15, change `export OPENWEBUI_IMAGE_TAG="0.8"` → `export OPENWEBUI_IMAGE_TAG="latest"`

## Verification
- [x] Run `source oow_ctl.sh && echo $OPENWEBUI_IMAGE_TAG` → should print `latest`
- [x] Run `docker compose config` → image should resolve to `openwebui/open-webui:latest`
