# Plan: Add "top" command to run_ollama_open_webui.sh

## Context
Add a `top` subcommand that runs `docker compose top`, passing through any extra arguments.

## Changes to `run_ollama_open_webui.sh`

- [x] Add a `top)` case with `shift` and `docker compose -f "$COMPOSE_FILE" top "$@"`
- [x] Update the usage message to include `top`

## Verification
- [x] Run `./run_ollama_open_webui.sh top` and confirm it works without variable warnings
