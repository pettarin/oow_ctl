# Plan 012: Rename OLLAMA_IMAGE and OPENWEBUI_IMAGE to *_IMAGE_NAME

## Context
Rename `OLLAMA_IMAGE` to `OLLAMA_IMAGE_NAME` and `OPENWEBUI_IMAGE` to `OPENWEBUI_IMAGE_NAME` for clarity and consistency with the existing `*_IMAGE_TAG` variables.

## Changes to `oow_ctl.sh`

- [x] Line 9: rename `OLLAMA_IMAGE` to `OLLAMA_IMAGE_NAME`
- [x] Line 14: rename `OPENWEBUI_IMAGE` to `OPENWEBUI_IMAGE_NAME`

## Changes to `docker-compose.yml`

- [x] Line 3: change `${OLLAMA_IMAGE}` to `${OLLAMA_IMAGE_NAME}`
- [x] Line 19: change `${OPENWEBUI_IMAGE}` to `${OPENWEBUI_IMAGE_NAME}`

## Changes to `README.md`

- [x] Rename `OLLAMA_IMAGE` to `OLLAMA_IMAGE_NAME` in the Configuration table
- [x] Rename `OPENWEBUI_IMAGE` to `OPENWEBUI_IMAGE_NAME` in the Configuration table

## Verification
- [x] `grep 'OLLAMA_IMAGE[^_N]' oow_ctl.sh docker-compose.yml README.md` returns no matches
- [x] `grep 'OPENWEBUI_IMAGE[^_N]' oow_ctl.sh docker-compose.yml README.md` returns no matches
- [x] Configuration table in README.md remains sorted lexicographically
- [x] `./oow_ctl.sh status` works as before
