# Plan: Cosmetic changes — rename script and sort variables/subcommands

## Context
Cosmetic cleanup: shorter script name and alphabetical ordering for readability.

## Changes

### Rename script
- [x] Rename `run_ollama_open_webui.sh` to `oow_ctl.sh`

### Sort env vars alphabetically in `oow_ctl.sh`
- [x] Reorder to: `OLLAMA_CONTAINER_NAME`, `OLLAMA_IMAGE`, `OLLAMA_IMAGE_TAG`, `OLLAMA_PORT`, `OLLAMA_VOLUME`, `OPENWEBUI_CONTAINER_NAME`, `OPENWEBUI_IMAGE`, `OPENWEBUI_IMAGE_TAG`, `OPENWEBUI_PORT`, `OPENWEBUI_VOLUME`

### Sort subcommands in usage message
- [x] Change to `{df|logs|start|status|stop|top}`

### Sort case statement branches alphabetically
- [x] Reorder cases to: `df`, `logs`, `start`, `status`, `stop`, `top`, `*`

### Prefix echo messages with log level tags
- [x] Prepend `[INFO]` to informational messages (start, stop, df labels)
- [x] Replace `WARNING:` with `[WARN]` in the df existence check

## Verification
- [x] Run `./oow_ctl.sh df` to confirm script works under new name and shows `[INFO]`/`[WARN]` prefixes
