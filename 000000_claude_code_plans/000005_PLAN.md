# Plan: Change volume mount point prefix from /mnt/data to /mnt/usb

## Context
Update the Docker volume host paths from `/mnt/data/docker/` to `/mnt/usb/docker/`.

## Changes to `run_ollama_open_webui.sh`

- [x] Change `OLLAMA_VOLUME="/mnt/data/docker/ollama"` to `OLLAMA_VOLUME="/mnt/usb/docker/ollama"`
- [x] Change `OPENWEBUI_VOLUME="/mnt/data/docker/open-webui"` to `OPENWEBUI_VOLUME="/mnt/usb/docker/open-webui"`

- [x] Refactor `df` command to loop over `/`, `$OLLAMA_VOLUME`, `$OPENWEBUI_VOLUME` with existence check before calling `df -h`
- [x] Add `logs` command that runs `docker compose logs` (ensures env vars are available)
- [x] Update usage message to include `logs`
- [x] Pass extra arguments (e.g. `-f`) from the script through to `docker compose logs` using `shift` and `"$@"`

## Verification
- [x] Run `./run_ollama_open_webui.sh df` — paths correctly show `/mnt/usb/...` (df fails as directories don't exist yet on this host)
- [x] Run `./run_ollama_open_webui.sh df` — confirms existence check works (WARNING for missing paths)
