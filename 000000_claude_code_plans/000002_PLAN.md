# Plan: Rename services and add named volumes

## Context
Rename Docker Compose service names and introduce a top-level `volumes` section with named volumes instead of direct bind mounts.

## Changes to `docker-compose.yml`

### Service renames
- [x] Rename service `ollama` to `svc-ollama`
- [x] Rename service `open-webui` to `svc-open-webui`
- [x] Update `depends_on` from `ollama` to `svc-ollama`
- [x] Update `OLLAMA_BASE_URL` from `http://ollama:11434` to `http://svc-ollama:11434`

### Named volumes
- [x] Add top-level `volumes` section with `volume-ollama` and `volume-open-webui`
- [x] Each volume uses the `local` driver with `device` pointing to `${OLLAMA_VOLUME}` / `${OPENWEBUI_VOLUME}` via bind mount options
- [x] Update service volume references to use `volume-ollama:/root/.ollama` and `volume-open-webui:/app/backend/data`

## Verification
- [x] Run `./run_ollama_open_webui.sh start` and confirm both containers come up
- [x] Run `./run_ollama_open_webui.sh status` to verify they're running
- [x] Run `./run_ollama_open_webui.sh stop` to confirm clean shutdown
