# Plan: Rename volume prefix from "volume-" to "vol-"

## Context
For consistency with the `svc-` prefix used on service names, rename the volume prefix from `volume-` to `vol-`.

## Changes to `docker-compose.yml`

- [x] Rename `volume-ollama` to `vol-ollama` in the `svc-ollama` service volumes reference
- [x] Rename `volume-open-webui` to `vol-open-webui` in the `svc-open-webui` service volumes reference
- [x] Rename `volume-ollama` to `vol-ollama` in the top-level `volumes` section
- [x] Rename `volume-open-webui` to `vol-open-webui` in the top-level `volumes` section

## Verification
- [x] Run `./run_ollama_open_webui.sh start` and confirm both containers come up
- [x] Run `./run_ollama_open_webui.sh status` to verify they're running
- [x] Run `./run_ollama_open_webui.sh stop` to confirm clean shutdown
