# Plan: Add "df" command to run_ollama_open_webui.sh

## Context
Add a new `df` subcommand to the management script that runs `df /` to check disk usage on the root filesystem.

## Changes to `run_ollama_open_webui.sh`

- [x] Add a `df)` case with three separate `df -h` calls for `/`, `$OLLAMA_VOLUME`, and `$OPENWEBUI_VOLUME`, each preceded by an echo label
- [x] Update the usage message to include `df`: `{start|stop|status|df}`

## Verification
- [ ] Run `./run_ollama_open_webui.sh df` and confirm it outputs disk usage for `/`
- [ ] Run `./run_ollama_open_webui.sh` with no args and confirm usage message includes `df`
